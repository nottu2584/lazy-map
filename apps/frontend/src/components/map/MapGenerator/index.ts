/**
 * MapGenerator component
 *
 * This is the main entry point for map generation UI.
 * All subcomponents (MapBasicSettings, SeedHistory, MapProgress, etc.)
 * are internal implementation details and should not be imported directly.
 *
 * Usage:
 *   import { MapGenerator } from '@/components/map/MapGenerator';
 */
export { MapGenerator } from './MapGenerator';
