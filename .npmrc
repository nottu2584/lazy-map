# Force pnpm usage
engine-strict=true
package-manager-strict=true

# Prevent npm from creating package-lock.json
package-lock=false

# Hoist dependencies to avoid deep node_modules
hoist-pattern[]=*
shamefully-hoist=true

# Use pnpm's strict peer dependencies
auto-install-peers=true
strict-peer-dependencies=false
