# Force pnpm usage
engine-strict=true
package-manager-strict=true

# Prevent npm from creating package-lock.json (pnpm uses pnpm-lock.yaml separately)
lockfile=true

# Hoist dependencies to avoid deep node_modules
hoist-pattern[]=*
shamefully-hoist=true

# Use pnpm's strict peer dependencies
auto-install-peers=true
strict-peer-dependencies=false
